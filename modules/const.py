from pathlib import Path

import numpy as np

EMBEDDINGS: dict[np.array, Path] = {

}

FILES: list[Path] = [
    Path('baza') / '2proc.txt',
    Path('baza') / 'bezwkladu.txt',
    Path('baza') / 'mdm.txt',
    Path('baza') / 'megahipo.txt'
]
